services:
  zanime:
    image: ghcr.io/ZortexSenpai/ZAnimeList:latest
    ports:
      - "8080:8080"
    volumes:
      - zanime_data:/data
    environment:
      - ConnectionStrings__Sqlite=Data Source=/data/zanime.db

volumes:
  zanime_data:
